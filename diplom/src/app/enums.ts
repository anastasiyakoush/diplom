export enum Component
{
    Obscheobrazovatelnyj = 0,
    Professonalnyj = 1,
}

export enum Status
{
  'не выбрано' = 3,
    'работает' = 0,
    'уволен' = 1,
    'декрет' = 2,
}

export enum Category
{
  'не выбрано' = 3,
    Высшая = 0,
    Первая = 1,
    Вторая = 2,
}

export enum Role {
  админ = 0,
  пользователь
}
